Write a query to report the information for all counties whose names start with "Ch"
SELECT name*
FROM countries 
WHERE country_name LIKE 'Ch%';

Write a query to find the id and parent_committee with name starting with "A" from the table committees. 
SELECT id, parent_committee
FROM committees
WHERE name LIKE 'A%';


Write a single query to report only the total number of the counties in 'Alabama'. 
SELECT COUNT(*)
FROM counties 
WHERE statecode = 'AL';

Write a single query to list only the population in year 2010 for the state represented by Jeff Sessions.
SELECT population AS populate_2010
FROM   statepopulation
WHERE  represented_by = 'Jeff Sessions';
